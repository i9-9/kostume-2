# Copia a .env.deploy y completa con tus datos
# .env.deploy está en .gitignore y no se sube al repo

# FTP (cPanel lo incluye por defecto)
FTP_HOST=ftp.kostumeweb.net
FTP_USER=uv3012
FTP_PASSWORD=
FTP_PATH=public_html
